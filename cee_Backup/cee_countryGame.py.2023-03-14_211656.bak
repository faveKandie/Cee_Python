import csv

list_data = []
temperature = []

with open("weather_data.csv") as file1:
    contents = csv.reader(file1)
    for row in contents:
        print(row)
        list_data.append(row)
        rate = list_data[row]
        print(rate[1])