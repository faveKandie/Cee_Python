import csv

list_data = []
temperature = []

with open("weather_data.csv") as file1:
    contents = csv.reader(file1)
    for row in contents:
        n = row
        print(row)
        list_data.append(row)
        rate = list_data[n]
        print(rate[1])