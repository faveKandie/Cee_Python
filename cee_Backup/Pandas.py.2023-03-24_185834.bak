import pandas

dict = {
    "name": ["uju", "cee", "ola", "david"],
    "Grade": ["99", "100", "85", "72"]
    }
date = pandas.DataFrame(dict)#reading from dictionary
    
date.to_csv("newfile.csv")

rate = pandas.read_csv("newfile.csv")
print(rate)
print(date)
